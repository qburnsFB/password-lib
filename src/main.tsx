import React from 'react'
import ReactDOM from 'react-dom/client'
import { EntryLibraryApp } from './EntryLibraryApp';

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <EntryLibraryApp />
  </React.StrictMode>
)
